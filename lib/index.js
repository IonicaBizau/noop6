"use strict";

/**
 * noop6
 * No operation as a module using arrow functions.
 *
 * @name noop6
 * @function
 * @param {Number} a Param descrpition.
 * @param {Number} b Param descrpition.
 * @return {Number} Return description.
 */
module.exports = function noop6 (a, b) {
    return a + b;
};